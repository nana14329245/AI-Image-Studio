"use client";
import { useRef, useState } from "react";
import PageHeader from "@/components/PageHeader";
const careers=["Office","IT","Banking","Hotel","Sales","Student"], backgrounds=["White","Gray","Blue","Office"], sizes=["Resume","1 × 1","Passport"];
export default function PortraitPage(){const [file,setFile]=useState<string|null>(null),[career,setCareer]=useState("Office"),[bg,setBg]=useState("White"),[size,setSize]=useState("Resume"),[generated,setGenerated]=useState(false),input=useRef<HTMLInputElement>(null);return <><PageHeader eyebrow="PROFILE / CAREER" number="TOOL / 004" title={<>A better first<br/>impression<span className="text-[#ed5127]">.</span></>} description={<>ปรับภาพโปรไฟล์ให้สุภาพและเหมาะกับบริบทการสมัครงาน พร้อมเลือกพื้นหลังและสัดส่วน</>}/><div className="page-wrap"><div className="grid border border-[#1d1e1b] lg:grid-cols-[1fr_390px]"><div className="p-6 lg:border-r border-[#1d1e1b]"><div className="mb-5 flex justify-between"><span className="micro">01 / PHOTO INPUT</span><span className="micro">PORTRAIT MODE</span></div><input ref={input} type="file" accept="image/*" className="sr-only" onChange={e=>{const f=e.target.files?.[0];if(f)setFile(URL.createObjectURL(f))}}/><button onClick={()=>input.current?.click()} className="dropzone w-full min-h-64"><span><span className="mb-4 block text-5xl">◎</span><span className="block">{file?"เปลี่ยนรูปโปรไฟล์":"Upload your photo"}</span><span className="mt-2 block text-xs text-[#6b6d65]">เลือกภาพที่เห็นใบหน้าชัดเจน</span></span></button><div className="mt-8 grid gap-8 md:grid-cols-3"><div><p className="micro mb-3">02 / CAREER</p><div className="grid gap-2">{careers.map(x=><button key={x} onClick={()=>setCareer(x)} className={`option ${career===x?"selected":""}`}>{x}</button>)}</div></div><div><p className="micro mb-3">03 / BACKGROUND</p><div className="grid gap-2">{backgrounds.map(x=><button key={x} onClick={()=>setBg(x)} className={`option ${bg===x?"selected":""}`}>{x}</button>)}</div></div><div><p className="micro mb-3">04 / SIZE</p><div className="grid gap-2">{sizes.map(x=><button key={x} onClick={()=>setSize(x)} className={`option ${size===x?"selected":""}`}>{x}</button>)}</div></div></div><button disabled={!file} onClick={()=>setGenerated(true)} className="btn-primary mt-8 w-full">{generated?"PORTRAIT READY":"CREATE PROFESSIONAL PHOTO  ↗"}</button></div><div className="p-6"><div className="mb-5 flex justify-between"><span className="micro">05 / RESULT</span><span className="font-mono text-xs">{career} / {bg} / {size}</span></div><div className="preview-card flex min-h-[560px] items-center justify-center p-8 bg-[#deded6]">{file&&generated?<div className="relative flex max-h-[500px] max-w-[350px] items-center justify-center overflow-hidden bg-white"><img src={file} alt="Professional portrait preview" className="max-h-[500px] max-w-full object-contain"/><span className="absolute bottom-3 left-3 border border-[#1d1e1b] bg-[#f3f3ee] px-3 py-2 micro">{bg} / {career}</span></div>:<div className="text-center"><div className="mx-auto mb-6 flex size-32 items-center justify-center border border-[#9da096] bg-[#f3f3ee] text-6xl font-light">◎</div><p className="text-lg font-medium">Professional, not artificial.</p><p className="mt-3 text-sm leading-6 text-[#6b6d65]">UI preview สำหรับขั้นตอน AI portrait ที่จะเชื่อมภายหลัง</p></div>}</div></div></div></div></>}
